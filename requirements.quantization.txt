# Optional GPU quantization backends.
# These packages compile CUDA extensions (notably GPTQModel),
# so ensure the CUDA toolkit is installed locally and CUDA_HOME
# points to its root (e.g., /usr/local/cuda) before installing.
auto-round>=0.2.0
gptqmodel>=1.4.3  # tip: pip install -v gptqmodel --no-build-isolation
autoawq>=0.2.0
